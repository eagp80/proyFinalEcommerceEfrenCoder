<div>
  <h1>
    PERFIL DEL USUARIO
  </h1>

  <p>Nombre: {{this.first_name}}, {{this.last_name}}</p>
  <p>Edad: {{this.age}}</p>
  <p>email: {{this.email}}</p>
  <p>role: {{this.role}}</p>
  <p>cart: {{this.cart}}</p>
  {{!-- <p>CarritoId: {{this.carrito.carritoId}}.
     Nombre producto: {{this.carrito.productos.[0].nombre}}.
     ProductoId: {{this.carrito.productos.[0].productoId}}.</p> --}}


  <div>
    <form action="/api/v1/session/logout" method="get">
      <br />
      <button type="submit">logout</button>
    </form>
  </div>

</div>